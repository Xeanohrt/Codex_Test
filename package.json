{
  "name": "phd-knowledge-wiki",
  "version": "1.0.0",
  "private": true,
  "description": "MEAN stack app for showcasing PhD projects and a public literature review with PDF chapter cross-references",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run start --workspace client\"",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
